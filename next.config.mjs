/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn-icons-png.flaticon.com'],
  },
 
  trailingSlash: true, // Optional: Ensure URLs have a trailing slash
};

export default nextConfig;
